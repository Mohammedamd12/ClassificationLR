# ClassificationLR
This project implements a binary classification model using Logistic Regression (LR) to predict the presence of lung cancer based on survey data.  The dataset contains multiple features . The goal of the project is to build a machine learning model that can classify whether a person is likely to have lung cancer.
